---
title: "Maladies cardiovasculaires : courbes ROC"
author: '___'
date: "___"
output: html_document
---

```{r setup, include=FALSE}
SciViews::R("ml")
```

# Objectif

Ce carnet de notes fait suite au document `cardio_proportions.Rmd`. L'objectif est de comparer un classifieur forêt aléatoire avec un classifieur machine à vecteurs supports en utilisant les courbes ROC et l'aire sous la courbe (AUC).

<!--% Ce projet est un projet cadré. Vous devez uniquement répondre à la question posée. Ne modifiez pas le nom des chunks. -->

# Analyses

<!--%  Importez le jeu de données `cardio.rds`. Assurez-vous que chaque variable soit dans un format correct. -->

```{r import}
cardio <-
cardio$id <- NULL
```

<!--# La visualisation des données a déja été réalisée dans le document cardio_proportions.Rmd -->

<!--% Divisez votre jeu de données en train et test (2/3 en apprentissage et en stratifiant cardio) -->

```{r split}
cardio_split <- 
cardio_train <- 
cardio_test <- 
```

## Classifieur forêt aléatoire

<!--% Réalisez un classifieur forêt aléatoire -->

```{r rforest}
cardio_rfor <- 
```

<!--% Calculez les probabilités d'appartenance aux classes à partir de votre classifieur en utilisant le set de test -->

```{r rforest_mem}
cardio_rfor_mem <- 
head(cardio_rfor_mem)
```

<!--% Calculez et affichez la courbe ROC pour ce classifieur en utilisant les métriques tpr et fpr -->

```{r rforest_roc}
cardio_rfor_predobj <- 

```

<!-- Calculez l'aire sous la courbe ROC -->

```{r rforest_auc}

```

## Classifieur machine à vecteurs supports

<!--% Calculez un second classifieur à l'aide de la machine à vecteurs supports à l'aide du même set d'apprentissage -->

<!--# La méthode SVM a beaucoup de paramètres à optimiser, mais dans le cadre de cet exercice, commencez par la version avec tous les paramètres par défaut, et donc, un noyau radial. S'il reste du temps quand vous avez tout fini, vous pouvez explorer d'autres valeurs des paramètres et comparer toujours avec courbe ROC les versions originale et optimisée (vous pouvez ajouter une troisième courbe sur le graphique final du document). -->

```{r svm}
cardio_svm <- 
```

<!--% Calculez les probabilités d'appartenance aux classes à partir de votre classifieur par validation croisée 10 fois -->

```{r svm_mem}
cardio_svm_mem <- 
head(cardio_svm_mem)
```

<!--% Calculez et affichez la courbe ROC pour ce classifieur en utilisant les métriques tpr et fpr -->

```{r svm_roc}
cardio_svm_predobj <- 
```

<!-- Calculez l'aire sous la courbe ROC -->

```{r svm_auc}

```

## Comparaison des deux classifieurs

<!--% Comparez les deux classifieurs en affichant leurs courbes ROC sur un même graphique -->

```{r roc_compa}

```

<!--% Interprétez ce graphique en 2 phrases. -->

## Discussion et conclusion

<!--% Indiquez ci-dessous en 3 phrases maximum quel classifieur vous considérez comme le meilleur, et pourquoi ? Est-ce que ce classifieur est meilleur en toutes circonstances (quelle que soit la prévalence de la maladie) ? -->
